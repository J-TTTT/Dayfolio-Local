# 日序 · 本地版

双击 **启动日序.cmd**，然后打开 http://127.0.0.1:4317 。首次使用填写称呼、邮箱以及至少 12 个字符的密码，创建管理员账号。这里的密码是本地账号的新密码，无需使用 ChatGPT 密码。

## 日常使用

- 保留日程、计划、工作记录、科研、灵感五种类型，以及项目名称、详细内容、进度、问题、解决方案、下一步。
- 灵感支持五个子分类和主题筛选。日报、周报按同名项目和记录日期整理，可复制或下载 Markdown。
- 与原站一样，摘要由已保存记录和关键词生成；没有外部 AI 请求、后台定时通知或邮件发送。修改记录会更新对应报表。每日新建记录可以保留项目连续进展。
- 「成员与邀请」中输入邮箱生成一次性邀请链接（7 天内有效），受邀者在这台电脑上打开链接，以匹配的邮箱设置密码。各账号记录独立，管理员可停用或恢复成员。
- 仅监听本机 127.0.0.1。其他电脑或手机不能通过此地址访问。账号、邀请和数据与 Sites 云端版分开管理。
- 关闭浏览器不会停止网站。双击 **停止日序.cmd** 才会停止服务；下次双击启动即可继续。电脑重启后需要再次启动。

## 数据保存与备份

记录、账号密码哈希和邀请保存在本目录的 `data/dayfolio.sqlite`。SQLite 运行时还可能有 `dayfolio.sqlite-wal` 与 `dayfolio.sqlite-shm` 文件，不要在运行时单独复制、移动或删除这些文件。

双击 **备份数据.cmd**，会创建一致的数据库快照到 `backups/`，服务运行时也可执行。备份包含所有本地账号的数据，妥善保管。登录状态也包含在快照中；本地电脑及数据库文件的持有人有能力读取数据库内容。

恢复备份：先停止服务，将整个 `data` 文件夹改名保留；创建新的 `data` 文件夹，把选中的备份复制进去并命名为 `dayfolio.sqlite`，然后启动。不要混用旧的 WAL/SHM 文件。

本地版首次为空库，**不会自动下载或同步 Sites 上已有的记录**。此前云端数据仍在原站，迁移需要另行导出与导入。

## 运行环境与源码

已构建的 `dist/` 前端和 `server/` 后端只需要 Node.js 24 或更新版本，运行时无需安装 npm 包或连接互联网。启动脚本优先使用 `DAYFOLIO_NODE`、本目录 `runtime/node.exe`、这台电脑现有的 Codex Node 运行环境，再尝试系统 Node。

也可以在本目录执行 `node server/server.mjs`，它会在前台运行，Ctrl+C 停止。修改源代码后需重新构建：安装 Node.js 24，执行 `npm ci`、`npm run build`；本机当前可复用上级目录已经安装的依赖。`npm test` 使用独立临时数据库，不接触正式记录。

主要文件：`app/workspace.tsx`（工作台）、`app/main.tsx`（本地账号和邀请）、`lib/journal.ts`（分类和报表）、`server/entries.ts`（记录接口）、`server/server.mjs`（本地服务）。日志位于 `logs/`。
